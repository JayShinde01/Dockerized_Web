# fastapi/Dockerfile
FROM python:3.9.12

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

# Use the runtime PORT env (fallback to 8000)
CMD ["sh", "-c", "uvicorn resume_matcher:app --host 0.0.0.0 --port ${PORT:-8000}"]
